# Database (Docker on port 5433 to avoid conflict with local PostgreSQL)
# Use these credentials when running the database with Docker Compose
DATABASE_URL="postgresql://restaurant_user:restaurant_password@localhost:5433/restaurant_reviews?schema=public"

# JWT Secret (generate a strong random secret)
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# App Environment
NODE_ENV="development"
